# CMD → PEVT 工作计划索引

原计划已按职责拆分。转换器不负责定义 PEVT 运行时语义，PolarisEvent 也不包含原版 CMD 的解析和兼容逻辑。

## 文档

1. [CMD转PEVT计划.md](CMD转PEVT计划.md)
   - 原版 CMD、MoveScript 和表达式的 AST；
   - CMD 到现有/新增 PEVT 语法的降低规则；
   - `.pevt`、`.pactor` sidecar 和批量调用图输出；
   - raw 回退、诊断和转换器验收标准。

2. [PEVT语义扩展计划.md](PEVT语义扩展计划.md)
   - PEVT 自身新增的通用语法与强类型能力；
   - 任意游戏值只读查询、场景实体、摄像机、立绘变换；
   - 时间轴调度、参数化 `callevt`、人物目录扩展与资源预载；
   - parser、binder、runtime、ownership、诊断和测试要求。

## 边界

- `AicCmdToPevt` 可以认识 `#MS_`、`TL`、`PIC` 等原版名称，但生成结果只能使用正式 PEVT 语法。
- PolarisEvent 不认识 `#MS_`、`TL`、`DOTL`、复合 PIC 字符串或其他 CMD 文本；新增能力必须具有与原版无关的通用语义。
- `$raw cmd` 只作为转换器在缺少等价能力时的保底产物，不属于新语义的实现方案。
- 两份计划通过 `PEVT-E01` 等能力编号衔接，避免复制并逐渐产生不一致的语义说明。

# PEVT / PolarisEvent 分阶段实现计划（Claude 执行版）

## 1. 使用方式

本文是新 PolarisEvent 的代码实施清单。执行者一次只完成一个编号阶段，不跨阶段预埋大段实现，也不在阶段未通过验证时继续下一阶段。

> 项目已经拆分为 `PolarisCore`、`PolarisEvent` 等组件项目。自阶段 11 起，本文中的旧物理路径和旧验证命令必须按 `PEVT-项目重构后Claude接手说明-阶段11.md` 映射；语义范围和阶段编号仍以本文为准。

> 2026-08-15 只读复核结论：阶段 1–10 的代码虽然能够构建且现有测试全绿，但没有完整满足本计划。阶段 11 暂停，必须先依次完成阶段 10A–10D 补正门。不得把当前阶段 8–10 状态继续称为“已固化”或“已验收”。

> 阶段验收和未来自动探针必须遵守 `PEVT-阶段验收与自动探针规范.md`。该规范采用失败关闭和独立反证复核；缺少证据时不得推进阶段游标。

需求真相按以下优先级读取：

1. PEVT-语法设计草案.md
2. PEVT-静态诊断表.md 与 PEVT-运行诊断表.md
3. PEVT-PolarisEvent实现总纲.md
4. PEVT-人物目录与原版别名规范.md
5. PEVT-嵌入注册与ID冲突规范.md
6. PEVT-同步指令中间层规范.md
7. PEVT-异步协程与等待模型.md
8. PEVT-内置事件语句表.md
9. PEVT-内置能力规范.md

规范冲突时不得静默选择。先在实现说明中指出冲突；若诊断表已经为某种行为分配编号，则以诊断表和语法规范为准。

## 2. 每次修改的硬限制

- 每阶段实际有效变更必须为 600–1000 行，建议落在 700–900 行。
- 统计生产代码、测试代码、项目文件和必要资源；不统计生成文件、锁文件、空白行以及为了凑行数写的重复注释。
- 使用 git diff --numstat 核对物理增删行，再人工排除生成物和无意义行。
- 自然工作量不足 600 行时，与本阶段明确列出的配套测试合并，不能填充无用抽象。
- 预计超过 1000 行时，只能在保持构建通过的内聚边界拆成 A/B 两次，并先更新本文预算；不能交付半个类型或破坏构建。
- 一个阶段只提交一种主要能力。禁止顺手实现后续阶段。
- 每阶段结束都必须给出：变更行数、改动文件、执行过的命令、测试结果、未完成项。
- 工作树可能含用户的其他未提交改动，只修改本阶段列出的路径，禁止还原或格式化无关文件。

### 2.1 逐项证据门（适用于阶段 10A 之后的全部阶段）

每个阶段开始时，先把该阶段每一个动词性要求拆成验收矩阵；阶段结束时逐行填写，不允许只写一段笼统总结：

| 计划要求 | 权威规范条目/诊断 | 生产代码入口 | 正向测试 | 反向/边界测试 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 一条要求一行 | 文档章节或编号 | 精确到类型/公开成员 | 精确测试名 | 精确测试名 | PASS/FAIL |

强制规则：

- `PASS` 必须同时有可定位的实现和能失败的测试证据；“已有类似代码”“语法树天然等价”“后续阶段会处理”均不能作为通过依据。
- 计划写“生成/产出/保存”的对象必须成为可检查的模型或公开只读结果；临时遍历、局部字典或注释说明不能替代交付对象。
- “绑定节点”不能用语法节点代替；“控制流边/已解析目标”不能用结构化 AST 的正文列表代替；“常量值”不能只用类型代替。
- “不可变”指深不可变：构造后不能通过原始集合、数组、字典或可变子对象改变结果。必须有从外部可变集合构造后再修改原集合的反向测试。
- 诊断编号登记、实际发射路径和断言该编号的测试是三件事。除计划明确写明“本阶段只登记、后续阶段发射”的编号外，缺任意一项都不得验收。
- 写明“逐编号测试”时，每个编号至少有一个直接断言；要求“正反测试”时，还要有一个不会误报该编号的合法邻近样例。
- 边界值必须列为数据行测试，不得只测典型值。数字至少包含最小值、最大值、刚好越界和与一元负号组合的情况。
- 任何公开构建入口必须强制执行它声称已经完成的阶段，或只接受不可伪造的前序验证结果；不能依赖调用方“记得先运行 Binder/Flow”。
- 如果实现者认为计划要求不合理或存在更好替代方案，必须在改代码前更新计划并获得用户确认。不得在源码注释中自行缩小阶段范围。
- 全部测试通过只表示已有断言没有失败，不等于阶段通过；最终结论以验收矩阵是否全为 `PASS` 为准。

### 2.2 阶段提交前的反向审计

每阶段除正常构建测试外，必须执行并汇报：

1. 搜索本阶段诊断编号，区分“仅目录存在”“生产代码发射”“测试直接断言”；
2. 搜索 `TODO`、`NotImplementedException`、`留给后续阶段`、`不在本阶段`、`天然就是` 等可能缩小交付范围的文字，并逐项解释；
3. 对计划要求的公开产物列出其数据成员，证明后续阶段无需重新扫描 token 或猜测语义；
4. 至少增加一个能击穿当前实现近似假设的反例，例如集合别名修改、边界溢出、遗漏调用前缀或调用方跳过某个分析器；
5. 对比阶段开始前后的诊断覆盖清单，不允许通过删除编号、降低级别或只改文档获得绿色结果。

### 2.3 阶段 11–48 的执行前细化规则

下文阶段 11–48 的条目是范围合同，不再被视为足以直接编码的全部细节。每次开始一个阶段前，Claude 必须先在交付说明中补出该阶段的“执行合同”，最少包含：

1. 本阶段新增或修改的公开类型、关键成员和数据所有权；
2. 输入、输出、失败结果、不可变性和取消边界；
3. 本阶段负责发射的全部诊断编号，以及明确延期编号的唯一负责阶段；
4. 与前后阶段交界的数据结构，证明后续阶段不需要重新解析、反射猜测或复制规则；
5. 至少一个正常样例、一个无效样例、一个边界样例和一个对抗样例；
6. 单元、集成、快照/golden、依赖扫描分别需要证明什么；
7. 明确的非目标和禁止替代方案；
8. 逐项证据矩阵及退出条件。

如果执行合同与权威规范或本计划冲突，先停止并报告，不能边实现边在注释里重新定义范围。阶段 11 已由 `PEVT-项目重构后Claude接手说明-阶段11.md` 提供详细合同，但仍须先完成 10A–10D；阶段 12 以后应在开始前生成同等颗粒度的接手说明。

### 2.4 后续阶段交界与唯一所有者

以下交界已经容易产生“双份实现”或互相等待，现固定唯一含义：

| 交界 | 前一阶段产物 | 后一阶段只允许增加的内容 | 禁止替代/重复 |
| --- | --- | --- | --- |
| 10C → 13 | 通用调用绑定接口、最小测试签名表、参数域接口 | 阶段 13 建立唯一权威 @ Descriptor 目录并接入具体参数域元数据 | 不保留两套 BuiltinApi/Descriptor 注册表；13 应迁移或扩展 10C seam |
| 11/12 → 27 | 深不可变人物目录数据、合并结果和 ID/参数域 | 阶段 27 只定义运行时查询服务接口和中立视觉租约 | 不重新定义第二个 `ActorCatalog` 数据模型；运行时接口建议明确命名为 `IPevtActorCatalogService` |
| 14 → 17 | 载荷格式、压缩/解压、哈希和限制 | 阶段 17 只负责调用共享前端与编码器生成 `.g.cs` | 生成器不得复制载荷编码或解析规则 |
| 15 → 17 | 游戏侧事件注册表、冲突规则和 registrar 契约 | 阶段 17 只生成该 registrar 的实现 | 生成器不得建立第二套运行时 ID 表 |
| 16 → 18 | 游戏侧人物注册表、合并与冲突规则 | 阶段 18 只生成 registrar 和资源访问器 | 工具侧不得自行决定与运行时不同的最终人物 ID |
| 17 → 39 | 17 可拒绝普通 PEVT 静态错误并保存 raw C# 源码 | 39 在共享层补上 PEVT8007–8015 C# 分析和缓存合同，工具/游戏随后共同调用 | 17 不得伪造 C# 分析；39 不得把通用 PEVT parser 复制进 RawCs |
| 24 → 25 | 同步命令帧、例程合同和仅供测试的暂停替身 | 25 提供正式 PevtWait 类型并接入跨帧恢复 | 24 不提前创建另一套生产等待层；25 不重写命令帧 |
| 26 → 34 | 通用确定性调度器、例程 ID 和所有权树 | 34 把 PEVT `async`/handler 语义映射到既有调度器 | 不新建第二个 async 调度器或 Task 旁路 |
| 27 → 28–32 | 中立原子服务接口、内存替身和调用记录 | 28–32 增加组合处理器及最窄游戏适配器 | 组合处理器不直接访问游戏 API，适配器不复制演出流程 |
| 29 → 30 | 统一只读 visual lease 与资源等待 | 30 只消费 lease 创建事件拥有的显示实例 | 30 不重新扫描 Bundle/PolarisRes 字段 |
| 33 → 34–40 | 唯一公开 PolarisEvent 宿主和 Tick 接入点 | 后续只注册能力/例程到该宿主 | 不新增第二个 `BepInPlugin`、第二个组件循环或旁路生命周期 |

若某阶段需要改变此表，必须先修改本表并说明迁移方案、旧入口删除方式和兼容测试，不能以“暂时并存”结束阶段。

## 3. 已完成的清场基线

旧链路已经删除，不属于后续实现范围：

- Polaris/Event 下旧 IPolarisEventRegistrar、PolarisEventDefinition、注册表、冲突守卫、运行时、Start/Change 外观和引用类型。
- Polaris/Patch/Patch_EV_getEventContent.cs。
- PolarisTools/Event/PolarisEventGenerator.cs。
- PolarisTools/Event/Language 下全部 HPP/.phxx 语言服务。
- 旧 .phxx 与事件别名模板。
- Plugin、PathsAPI、项目引用、VSIX 清单和菜单中的旧入口。

后续任何阶段都不得恢复以下默认执行链：

    .phxx -> commandText -> 磁盘 .cmd -> EV.getEventContent -> EV.stack

原版 EV 文本解释入口只允许在阶段 38 的 $raw cmd 独占桥中出现。阶段 29 的 `game-pxls` 资源桥可以复用原版资源加载类，但不能创建 EvReader 或提交 CMD 文本。

## 4. 目标代码布局

- Polaris/Polaris.Pevt.Core：netstandard2.0，共享语言、绑定、便携解释器与调度模型；不得引用 Unity、BepInEx、游戏程序集或 Visual Studio SDK。
- Polaris/Polaris.Pevt.Core.Tests：语言和便携运行时单元测试。
- Polaris/Event/Pevt：游戏侧宿主、注册扫描、生命周期和游戏原子服务适配器。
- Polaris/Event/Pevt/Content：Polaris 内置 `.pactor` 人物固定目录。
- Polaris/Polaris.Pevt.IntegrationTests：可引用 Polaris 的运行集成测试与游戏接口替身。
- PolarisTools/Event/Pevt：.pevt 生成器与 Visual Studio 语言服务。
- PolarisTools/Event/Actors：.pactor 生成器、参数编辑器与 PolarisRes 资源选择器。

Polaris.csproj 必须显式排除 Core 与测试项目的源码通配，再以 ProjectReference 引用 Core。PolarisTools 也引用同一 Core，不允许复制解析器。

## 5. 全局不变量

- PEVT 永远以 UTF-8 原始源文本为真相，不生成 PEVT 字节码，不写回 .cmd。
- 游戏侧必须对嵌入源重新做完整静态校验，不能信任工具侧结果。
- 解释器只执行已绑定的不可变节点；除 exec 外不在每帧重新解析。
- 五种普通类型只有 int、float、bool、char、string；handler 是运行时专用包装，不进入普通类型系统。
- 表达式按规范的从左到右规则执行，括号形成强制子表达式；不得套用 C# 运算符优先级。
- 同步和异步 @ 调用复用同一个协程工厂，不能复制业务处理器。
- 全部跨帧等待必须表现为可取消、可诊断的 PevtWait。
- callevt 只在运行时查询全局 ID；目标不存在不是构建错误。
- exec、原始桥和子事件都必须进入同一所有权树、预算、诊断与清理边界。
- 静态和运行诊断使用规范中已有编号；禁止为同一错误临时另造编号。
- 普通 PEVT 代码不能获得 Unity 对象、游戏对象、反射入口或任意 C# 方法调用能力。
- 人物公开 ID 只能来自独立人物目录；原版 CMD 人物短键和 TALKER_REPLACE 临时键不得泄漏到普通 @ 参数。
- `.pactor` 是 XML 数据，不执行表达式、流程或任意 C#；自定义视觉只能引用允许的 PolarisRes 静态资源字段。
- 原版 game-pxls 资源是借用，自定义 PolarisRes 资源由其来源 owner 持有；事件只拥有显示实例和临时等待。

## 6. 通用验证命令

阶段 1 创建项目后，每次至少运行：

    dotnet test Polaris.Pevt.Core.Tests/Polaris.Pevt.Core.Tests.csproj
    dotnet build Polaris.csproj --no-restore
    dotnet build ../PolarisTools/PolarisTools.csproj --no-restore -p:DeployExtension=false

建立集成测试项目后再增加：

    dotnet test Polaris.Pevt.IntegrationTests/Polaris.Pevt.IntegrationTests.csproj

每次都运行旧链路回归扫描：

    rg -n -g "!*.md" "commandText|\.phxx|Polaris\.Event\.Compiler|HppCompiler|EventsDir|Patch_EV_getEventContent" .

命令应无命中；阶段 38 新增的原始 EV 桥必须用精确白名单路径复核，不能放宽为全项目允许。

## 7. 宏观阶段门

| 阶段门 | 覆盖阶段 | 必须证明的结果 |
| --- | --- | --- |
| A：语言闭环 | 1–10、10A–10D | 同一源文本能稳定产生 token、深不可变 AST、绑定程序、显式控制流模型和规定诊断；10A–10D 全部通过前，A 门不得关闭。 |
| B：目录与分发闭环 | 11–18 | `.pactor` 与 `.pevt` 均能用共享规则生成强类型注册代码，游戏侧按独立全局空间登记。 |
| C：同步解释闭环 | 19–27 | 不接游戏 API 也能用替身完整运行流程、块、同步 @、人物目录解析与失败诊断。 |
| D：可演出 P0 | 28–33 | 对话、选择、原版/自定义人物立绘、图层、画面、音频和 UI 可由真实适配器执行并清理。 |
| E：异步与组合 | 34–37 | handler、await、all/any、callevt 与 exec 具有确定调度和所有权。 |
| F：逃生口与全能力 | 38–43 | raw 桥、P1/P2 原子服务可控，普通代码仍不依赖原版 EV 文本解释器。 |
| G：工具与发布 | 44–48 | `.pevt/.pactor` 编辑器与运行时共用规则，全链路、性能、诊断和清理达到发布门槛。 |

### 7.1 阶段 1–10 复核归因（2026-08-15）

本次复核不能简单归结为“计划没写清”或“实现者理解错”，实际分为三类：

| 缺口 | 原计划是否明确 | 归因 | 后续处理 |
| --- | --- | --- | --- |
| 阶段 10 没有语句索引、边、switch/goto 已解析目标和绑定节点 | 明确，原阶段 10 逐项列出 | 实现偏离；用结构化 AST 替代计划明确要求的控制流和绑定产物 | 10D 必须实现真实模型与 golden 图 |
| 阶段 9 集合等待、PEVT7203、PEVT7403 及参数域接口未完成 | 参数域与主要调用绑定明确；但“全部 PEVT8xxx”又与阶段 39 负责 PEVT8007–8015 冲突 | 主要是实现提前封板，同时原计划的 Raw C# 诊断归属也有矛盾 | 10C 补齐调用绑定并建立编号归属账本；PEVT8007–8015 明确归阶段 39 |
| 阶段 8 缺常量值、绑定节点、快照复制契约及逐编号测试 | 常量值和逐编号明确；“实现快照语义”与阶段 19/20 才执行值复制存在阶段归属含混 | 实现把“类型检查通过”误当作绑定产物，原计划也没有区分静态复制标记与运行行为 | 10B 补齐独立 Bound 模型和复制契约；阶段 19/20 验证真实运行快照 |
| AST 只读属性但保留可变集合别名 | “不可变”已写，但未定义深不可变验法 | 计划验收定义不足，同时实现采用了过浅理解 | 10A 增加防御复制及别名攻击测试；通用门定义深不可变 |
| `2147483648` 延迟后没有闭合检查 | 只写 int32 范围和一元负号，没列四个边界组合 | 计划边界样例不足，实现在 Lexer 与 Binder 间断链 | 10A 用数据行固定边界语义 |
| 一行一语句仅检查部分关键字起始 | 规则明确，但没有列 `@`、赋值、label、raw 等反例 | 计划测试描述过泛，启发式实现漏覆盖 | 10A 改为基于完整语句边界并加入反例矩阵 |
| 文件 `id` 没有执行 PEVT1110/1111 | 权威语法/诊断表明确，阶段 6 摘要未点名 | 计划阶段摘要不够具体，实现者也未执行规范追踪 | 10A 补齐；以后矩阵必须把规范编号映射到代码和测试 |
| 阶段 7 若干精确诊断缺发射/测试 | 只写“合法与非法样例”，没有逐编号要求 | 计划验收颗粒度不足 | 10A/10C 按诊断所属层补齐 |

因此：阶段 8–10 的主体问题不是计划遗漏，而是没有按已写要求交付；阶段 3–7 同时暴露了验收措辞不够可执行。后续阶段统一受 2.1、2.2 的证据门约束，不能再凭概括性 bullet 和测试总数封板。

## 8. 逐阶段实施

### 阶段 1：共享项目骨架与源码基础设施（700–850 行）

- 创建 Core、Core.Tests 和 IntegrationTests 项目，配置 netstandard2.0 与测试目标框架。
- 修改 Polaris.csproj 和 PolarisTools.csproj，只增加正确的排除与 ProjectReference。
- 实现 SourceText、TextSpan、TextLocation、LineMap、严格 UTF-8 解码和取消检查。
- 实现诊断严重级、诊断对象、诊断包和最小 DiagnosticBag。
- 测试 CRLF/LF、中文列号、BOM、非法 UTF-8、跨度越界和不可变性。
- 验证：三个项目可恢复并构建；Core 程序集依赖列表不含 Unity、游戏程序集或 VS SDK。

### 阶段 2：静态诊断目录与 token 模型（650–800 行）

- 把 PEVTxxxx 的编号、名称、级别和默认消息集中实现为唯一目录。
- 实现 SyntaxKind、SyntaxToken、Trivia、TokenValue 与缺失 token 表示。
- 建立关键字、类型名、enable 能力和保留字表。
- 诊断目录测试必须逐项核对规范已有编号，检测重复编号与重复名称。
- 验证：反射枚举诊断目录所得集合与静态诊断表固定快照一致。

### 阶段 3：基础词法器（850–980 行）

- 识别换行、空白、标识符、关键字、标签、@ 名称、整数、浮点、bool、char、string 和运算符。
- 实现 int32/float32 字面量范围检查、字符长度、标准转义和一元负号 token 化。
- 支持 // 与 /* */ 注释，保留准确 trivia 和源码范围。
- 不解析语法结构，不提前绑定类型。
- 验证：表驱动词法测试覆盖 PEVT1001–1010 及所有基本 token。

### 阶段 4：特殊文本词法与行规则（800–950 行）

- 实现三单引号 raw 块、结束分隔符转义和左侧紧贴约束所需 token 信息。
- 实现字符串加号换行拼接及列对齐规则，禁止 tab、空行和插入注释。
- 实现一行一语句、分号禁止和非法普通跨行的词法/预解析诊断。
- raw 内容保持原字节对应的字符文本与完整 span，不做变量插值。
- 验证：覆盖 PEVT1005–1013、PEVT8003–8005、8016–8017 的 golden 测试。

### 阶段 5：表达式语法树与解析器（850–980 行）

- 建立不可变 SyntaxNode、表达式节点、参数列表、括号、转换、一元和链式二元节点。
- 按 PEVT 线性从左到右规则解析，括号递归形成独立子表达式。
- 解析赋值右值、@ 调用、_ 块调用、status、await 单句柄和 raw cs 表达式占位。
- 错误恢复以物理行和闭合符为同步点，保证一次加载可报告多个错误。
- 验证：AST 快照证明 a + b * c 是线性链，a + (b * c) 才形成嵌套。

### 阶段 6：文件头、声明与结构化流程解析（900–1000 行）

- 解析 id、enable、var、const、赋值、if/elif/else/endif、while/endwhile。
- 解析 switch/case/default/endswitch、标签、goto 与 end。
- 文件头强制 id 第一，enable 只能紧随其后且不进入执行语句。
- 结构节点保存正文列表和闭合 token；空正文仍构造节点并留给诊断。
- 验证：最小合法文件、每类孤立闭合符、错位分支和嵌套恢复快照。

### 阶段 7：块、事件调用、异步与动态语法解析（900–1000 行）

- 解析自定义块签名、参数、返回类型、return、endblock 及定义后调用。
- 解析 handler 声明、async @/_ 调用、await、await all/any、kill、status。
- 解析同步/异步 callevt、exec、$raw cmd 和带传入变量列表的 $raw cs。
- 明确自定义块中的 end 只形成错误节点，不终止块。
- 验证：覆盖语法规范第 10–15 节的合法与非法样例快照。

### 阶段 8：符号、类型与表达式绑定（900–1000 行）

- 实现 PevtType、常量值、变量/常量/参数/块/句柄符号和词法环境。
- 绑定五种普通类型、显式转换、同型二元运算、通用等值和数字大小比较。
- 在绑定节点中实现“先求值再复制入槽”的快照契约、只读常量和未初始化状态的静态可用性检查；真实值复制与来源变量后续变化的运行行为由阶段 19/20 验证。
- 一元负号只接受数字；减号有左操作数时保持二元运算。
- 验证：表达式类型矩阵和 PEVT5xxx/6xxx 逐编号测试。

### 阶段 9：名称、调用与能力绑定（850–980 行）

- 实现变量定义先于使用、自定义块自己的环境和定义先于调用。
- 绑定块参数、返回路径约束、@ 完整签名重载、返回接收类型和参数域接口。
- 绑定 enable cs、enable async、raw 参数副本和 handler 专用规则。
- callevt 只绑定语法与返回上下文，不查询任何真实事件 ID。
- 验证：本阶段负责的 PEVT7xxx 与 PEVT8xxx 诊断均有正反测试；未知 callevt ID 静态通过。PEVT8007–8015 的 C# 语义分析明确归阶段 39，本阶段只保存 raw 源码、绑定传入变量并检查能力/外围语法，不伪造 C# 分析结果。

### 阶段 10：静态控制流与不可变程序定义（900–1000 行）

- 生成语句索引、分支边、循环边、switch case 目标、标签和已解析 goto 目标。
- 检测所有事件路径是否到达 end、块返回路径、不可达语句和空流程正文警告。
- goto 只允许规范边界；不尝试证明 while/goto 是否形成死循环。
- 产出 Immutable PevtProgramDefinition，包含能力、符号槽、绑定节点、源码映射和哈希。
- 验证：控制流 golden 图覆盖嵌套结构、向后 goto、switch 专用 goto 和不可达警告。

### 阶段 10A：词法、语法与深不可变补正（700–950 行）

本阶段只修复阶段 3–7 的前端契约，不进入绑定、人物目录或运行时。

- 完成文件 `id` 的 PEVT1110/1111：空 ID 报错；字符只接受 `a-zA-Z0-9` 与规范规定的中文汉字。测试空值、ASCII、中文、混合、标点、空格和其他 Unicode 字母。
- 闭合 int32 边界：`2147483647` 合法、`2147483648` 非法、`-2147483648` 合法、`-2147483649` 非法；额外测试括号、双负号、二元减号与空白，保证延迟幅值不会以 `TokenValue.None` 混入合法绑定程序。
- 把“一行一语句”从关键字启发式提升为完整语句边界规则。至少覆盖 `@a() @b()`、`x = 1 y = 2`、`await a kill b`、label/goto、callevt、raw、块调用和结构闭合符；合法的参数、子表达式和 raw 内容不得误报。
- 补齐属于词法/语法层、目前只有目录定义的精确诊断发射；至少逐项审计 PEVT1001–1013、1101–1201、2001–2414、3001–3002、3101–3103、3105、5001–5003、5010、5022–5023、6013、7001–7003、7119、7216、8001。PEVT2415 及需要符号/目标解析的标签、goto、路径诊断分别归 10C、10D，不能在 parser 中靠文本猜测。
- 所有 SyntaxNode、SyntaxToken、Trivia 及其集合做深不可变处理：防御复制或不可变集合；不允许保留调用者可变集合别名。
- 语法恢复仍须在一次解析中报告多个错误；不得为了发射精确编号破坏后续行恢复。
- 验收测试必须直接断言本阶段触及的每个诊断编号，并增加 AST/Token 集合别名修改攻击测试。
- 退出条件：阶段 10A 验收矩阵全为 PASS；两个目标构建；现有测试和新增测试全绿；没有把任何语法诊断延期到人物或运行阶段。

### 阶段 10B：常量值、绑定节点与类型语义补正（850–1000 行）

本阶段完成原阶段 8，不处理调用重载、参数域和控制流。

- 建立独立于 SyntaxNode 的深不可变 BoundNode/BoundExpression/BoundStatement 基础模型；每个绑定节点保存结果类型、必要的已解析符号和源码映射。
- 建立五种普通类型的 `ConstantValue` 表示，明确无值与未初始化不是第六种 PEVT 类型；常量符号保存绑定后的值或可明确求值的常量表达式结果。
- 在 Bound 声明、初始化和赋值节点中显式编码“先求值再复制入槽”的快照契约；10B 测试验证绑定形状和符号不会退化为实时别名。真实执行时来源变量变化不影响目标值、字符串/载体不泄漏引用的行为测试唯一归阶段 19/20。
- 完整实现只读常量、赋值目标、未初始化、环境隔离、显式转换、一元/二元运算和错误类型传播，避免同一根错误产生无界级联诊断。
- 对 PEVT5xxx、PEVT6xxx 建立目录—发射点—正向—反向四列覆盖测试；除规范明确属于后续调用/运行阶段的编号外不得为空。重点补齐 PEVT5002/5003/5006/5010/5022/5023、6005/6011/6013。
- Binder 的结果必须返回绑定节点或显式失败结果，不能只向 `DiagnosticBag` 写入后丢弃语义信息。
- 退出条件：表达式类型矩阵、边界值和快照测试全绿；所有成功绑定的普通表达式都有非 Error 的绑定节点；不得以原始 AST 冒充绑定结果。

### 阶段 10C：名称、调用、能力与异步静态绑定补正（850–1000 行）

本阶段完成原阶段 9；Raw C# 只做语法规范要求的静态契约，不实现运行时执行器。

- 完成变量/块定义先于使用、块独立环境、参数与返回契约、`@` 重载匹配、返回接收类型和 raw 参数副本绑定，并把解析结果写入 Bound 模型。
- 实现可扩展的参数域接口。阶段 10C 至少提供不依赖人物目录的通用域契约与测试替身；人物/Appearance 等具体域由阶段 12/13 接入，不能把接口本身延期。
- 完成 PEVT7203；完成 `await all/any` 的句柄重复、绑定数量、绑定名重复、返回能力和嵌套表达式限制；所有 handler 操作记录已解析句柄符号及预期返回类型。
- `exec` 参数必须静态为 string，发射 PEVT7403。`callevt` 只绑定形状、能力和返回上下文，仍禁止查询真实事件 ID。
- `enable cs`、`$raw cs` 参数副本、外围语法、能力和源码保存必须完成；PEVT8007–8015 的 C# 语法、返回类型和所有路径 return 分析唯一归阶段 39，10C 不发射、不伪造，也不得把它们计入 10C 完成率。
- 对 PEVT7xxx、PEVT8xxx 生成自动覆盖与归属清单，每个属于阶段 9/10C 的编号必须有直接正反测试；PEVT8007–8015 标记为阶段 39，其他延期编号必须写出唯一负责阶段，不得空白。
- 删除或改写 Binder 中与本阶段交付相冲突的“留给后续”说明；源码注释必须描述真实状态，不能代替计划变更。
- 退出条件：没有未解释的 PEVT7xxx/8xxx 覆盖空洞；未知跨模组 callevt 静态通过；所有成功调用节点保存选中的签名、参数绑定和返回契约。

### 阶段 10D：显式控制流模型与不可变程序定义补正（900–1000 行）

本阶段完成原阶段 10，并重新关闭语言闭环 A。

- 为外层事件和每个自定义块分配稳定语句索引；索引顺序必须有文档化规则且可由 golden 测试固定。
- 生成显式控制流模型，至少包含顺序边、if/elif/else 分支边、while 进入/回边/退出边、switch case/default/退出目标、return/end 终结边和不可达节点信息。
- 标签与 `goto #label` 必须解析为具体目标索引；switch 专用表达式 goto 必须解析为当前 switch 的具体 case 目标；禁止只在分析器局部字典中短暂解析后丢弃。
- 用显式图执行所有路径到 end、块返回路径、不可达语句和空正文分析；保持“不证明 while/goto 死循环”的既定保守规则。
- PEVT3003、3104、3106、3107、3111、3112、4001、4002 必须各有实际发射点和直接正反测试；结构性空正文 warning 保持由语法节点精确定位。
- 重建深不可变 `PevtProgramDefinition`，至少包含事件 ID、能力、完整符号槽、Bound 根节点、控制流模型、已解析目标、源码映射和源码哈希。语法树可以附带用于工具显示，但不能代替 Bound/Flow 数据。
- 程序定义创建入口必须自己执行完整 Parse→Bind→Flow 管线，或只接受内部不可伪造的成功分析结果；增加“调用方跳过 Binder/Flow 仍不能构造有效定义”的测试。
- 对所有公开列表、字典、节点和槽做深不可变攻击测试；修改构造时传入的原始集合不得改变程序定义。
- golden 图至少覆盖嵌套结构、向前/向后 goto、跨环境非法 goto、switch 专用 goto、不可达 warning、块返回和警告不阻止定义生成。
- 退出条件：后续解释器无需重新扫描 token、重新查标签或从 AST 猜测跳转；语言闭环 A 的证据矩阵全部 PASS 后，才允许开始阶段 11。

### 阶段 11：人物目录模型、XML 读取与诊断（850–1000 行）

- 在 Core 实现 ActorCatalog、Actor、WorldSprite、Portrait、UiPortrait、Appearance、Anchor 与视觉提供者不可变模型。
- 实现严格 `.pactor` XML reader：命名空间、Version、目录 namespace、局部 ID、颜色、重复项和引用完整性。
- 按人物规范把 `PEVT9101–9118` 同时加入静态诊断文档、DiagnosticCatalog 与逐项快照，保持阶段 2 的目录测试继续全绿。
- XML 不接受 DTD、外部实体、任意类型名、方法名、条件或未知可执行元素；保留文件级源码位置用于诊断。
- 验证：合法单/多人物目录往返、恶意 XML、版本、重复 visual、无效默认 portrait、anchor 非有限数与全部新编号。

### 阶段 12：人物合并、原版固定映射与参数域（800–950 行）

- 实现人物最终 ID `<namespace>:<local-id>`、目录合并、内置 namespace 封闭、同源/跨源冲突候选模型。
- 读取 `AliceInCradle.BuiltinActors.pactor`，验证 18 个原版稳定说话人键归并为 16 个公开 profile（含无立绘的叙述者）；Noel 三套 portrait 必须固定映射。
- 明确 TALKER_REPLACE 动态键不进入目录；LegacyPerson 只允许受信任内置来源。
- 实现 ActorId、AppearanceId、ActorAnchor 参数域 provider；未知跨模组人物字符串只给补全缺失提示，不生成 `.pevt` 静态错误。
- 验证：固定目录 golden、Ordinal 大小写、保留 aic、动态键黑名单、同人物 visual 查询和自定义 anchor 覆盖。

### 阶段 13：@ API 描述目录与构建期契约（700–850 行）

- 把阶段 10C 的最小测试签名 seam 迁移/扩展为唯一 CommandDescriptor 目录，包含参数描述、返回类型、参数域、等待类别和是否可并行；阶段结束不得并存两套调用查询入口。
- 把 P0/P1/P2 API 表全部登记为元数据，人物参数连接阶段 12 的目录参数域，暂不接游戏处理器。
- 同名重载必须以参数数量和完整类型唯一选中。
- 为可并行 API 自动校验对应 _start 签名契约，但不复制描述数据。
- 验证：API 表快照、重载唯一性、返回类型、人物域与其它参数域完整性测试。

### 阶段 14：嵌入载荷编码与完整性校验（750–900 行）

- 实现 PevtEmbeddedSource、严格 UTF-8、GZip Base64 v1、SHA-256 与长度硬上限。
- 保留原始换行，不格式化源文本；SourcePath 只允许项目相对路径。
- 解码错误、膨胀、长度、哈希和格式版本失败产生明确加载结果。
- 编码器与解码器放在 Core，使 Tools 与游戏侧使用同一格式。
- 验证：往返、损坏 Base64、截断 GZip、伪造长度、压缩炸弹边界测试。

### 阶段 15：事件运行时注册表、扫描器与 ID 冲突守卫（850–1000 行）

- 在 Polaris/Event/Pevt 建立 IPevtRegistrar、特性、注册上下文、扫描器与内存 /event 表。
- 运行时解包后再次调用 Core 完整加载，并核对 DeclaredId 与源码 id。
- ID 使用 Ordinal 全局比较；跨程序集先注册保留并汇总致命冲突。
- 同程序集重复警告并由后注册覆盖，保留两条相对路径与哈希。
- 验证：模拟多个程序集来源，覆盖损坏包、大小写 ID、中文 ID 和扫描 Seal 后冲突。

### 阶段 16：人物运行时注册表、扫描器与冲突守卫（850–1000 行）

- 在 Polaris/Event/Pevt/Actors 建立 IPevtActorRegistrar、特性、来源固定的注册上下文与只读 ActorRegistry。
- 将内置 `.pactor` 作为 Polaris EmbeddedResource 优先解析登记，再扫描外部生成注册器；外部不能伪造 BuiltIn 或 aic namespace。
- 人物冲突与事件冲突分表收集、分开 Seal；跨程序集冲突映射 PEVTR4404 并列出双方路径与哈希。
- 注册项只保存不可变资料和延迟视觉访问器，不在扫描期触发 PolarisRes PXLS 加载。
- 验证：内置 15 人物、跨程序集冲突、卸载撤销、访问器未提前求值、事件与人物同名互不干扰。

### 阶段 17：PolarisTools `.pevt` 生成器与模板（850–1000 行）

- 新建 .pevt 单文件生成器，调用 Core 校验、编码并生成只提交载荷的 .g.cs。
- 生成内容包含格式、ID、相对路径、长度、哈希和分段 Base64，不包含 AST/CMD。
- 增加 .pevt 项模板、VSIX 资产和自动 GeneratorBinding。
- 同项目重复 ID 先实现可测试的项目扫描服务，再报告构建错误。
- 验证：固定输入的生成 C# 快照、可编译性、重复 ID 和非法源码拒绝测试。

### 阶段 18：PolarisTools `.pactor` 生成器、模板与资源扫描（900–1000 行）

- 新建 `.pactor` 单文件生成器，调用 Core XML reader 并生成 IPevtActorRegistrar `.g.cs`，每个资源只生成强类型延迟字段访问器。
- 抽取并复用 PUI Image 的项目定位、注释/字符串掩码、特性字段扫描和 FileSystemWatcher；扩展识别 MImage 与 PxlsCharacterHandle。
- 校验字段 static/可见性、PolarisResourceFolder/PolarisResource 特性与视觉所需类型，报告 PEVT9110/9111/9117/9118。
- 增加 `.pactor` 项模板、VSIX 资产和 GeneratorBinding；同项目最终人物 ID 重复报告 PEVT9106。
- 验证：生成 C# 快照与可编译性、失效字段仍保留 XML、跨文件重复、嵌套资源类和扫描缓存失效。

### 阶段 19：运行值、环境与执行帧（800–950 行）

- 实现 PevtValue、未初始化槽、值复制、普通类型访问器和 handler 分离存储。
- 实现文件环境、块调用环境、声明执行标记和显式执行帧栈。
- 定义 ExecutionResult、暂停原因、终止原因和源位置调用栈。
- 不实现具体流程跳转或 @ 调用。
- 验证：值快照、字符串不可变、槽隔离、重复声明运行标记和深帧栈测试。

### 阶段 20：表达式与简单语句解释（850–1000 行）

- 执行字面量、读取、转换、一元/二元运算、赋值、var 和 const。
- int 使用 checked；除零、非有限 float 和未初始化读取映射到既有 PEVTR 编号。
- 每次声明节点在同一环境只能执行一次，重复执行产生 PEVTR3001。
- 每条语句消费确定步数，不依赖 C# 运算优先级。
- 验证：表达式绑定结果与运行结果配对测试，覆盖 PEVTR2xxx/3xxx。

### 阶段 21：结构化流程解释（850–1000 行）

- 执行 if/elif/else、while、switch/case/default 和正常顺序推进。
- switch 表达式只求值一次；case 按源顺序求值；命中后直接到 endswitch 后。
- 结构控制使用预绑定目标，不在运行时重新搜索 token。
- 死循环不静态拒绝，只允许执行预算在后续阶段终止。
- 验证：嵌套流程、case 运行同值首项胜出和无 default 路径。

### 阶段 22：标签、goto、end 与自定义块（900–1000 行）

- 执行预绑定标签 goto、switch 表达式 goto、自定义块调用和 return。
- 块参数按值快照，块环境隔离；有返回类型按绑定契约提交值。
- end 只允许终止最外层事件；块中的 end 永远不能进入可执行定义。
- 事件与块调用使用显式帧，不依赖 C# 递归。
- 验证：向前/向后 goto、块嵌套、提前 return、隐式无值 return 和深度边界。

### 阶段 23：运行预算、诊断与跟踪（700–850 行）

- 实现每帧步数、总步数、调用深度、无进展检测和可配置上限。
- 实现 PEVTR 诊断对象、事件/块调用栈、源码位置、内部异常链和日志投影。
- 提供只读语句跟踪与预算统计，不改变调度顺序。
- while/goto 超限统一产生 PEVTR1001，不要求分析具体循环形状。
- 验证：预算边界差一测试、无进展 PEVTR1002、诊断快照和跟踪开关等价性。

### 阶段 24：同步 @ 指令帧与内存替身（850–1000 行）

- 实现 IPevtCommandRoutine、PevtRoutineContext、Arguments、Result 和反向 Cleanup 栈。
- 解释器按已绑定 Descriptor 创建同步指令帧，实参先全部求值并快照。
- 用内存 FakeCommandRoutine 验证立即返回、测试替身暂停、失败和返回契约；正式跨帧等待类型只由阶段 25 提供。
- 此阶段不接 Unity，也不启动独立 handler。
- 验证：参数求值无副作用交叉、结果一次提交、PEVTR4001/4002 和清理逆序。

### 阶段 25：PevtWait 基类与内置等待（800–950 行）

- 实现 Pending/Succeeded/Failed/Cancelled 状态和幂等 Tick/Cancel。
- 实现 Frame、Predicate、Signal、Resource、Motion、Input、Composite 等待。
- Wait 不能暴露 Unity yield 对象；结果与异常通过统一接口读取。
- 给同步指令帧接入跨帧恢复。
- 验证：成功、失败、超时、取消、重复 Tick/Cancel 和完成后不再推进。

### 阶段 26：调度器、例程实例与所有权树（900–1000 行）

- 实现 PevtRoutineInstance、稳定递增 ID、确定调度顺序和同帧完成顺序。
- 根事件、附着同步例程、子例程和资源占用进入同一所有权树。
- 实现事件结束、替换、异常和卸载时的级联取消与清理超时。
- 捕获 MoveNext、Tick、Cancel 和清理异常。
- 验证：多例程同帧、取消竞态、子树清理和调度可重现性。

### 阶段 27：原子服务接口、人物解析与 P0 会话状态（800–950 行）

- 定义 `IPevtActorCatalogService`、Clock、Resources、Dialogue、Choice、Portrait、Image、Screen、Camera、Effect、Audio、Music、Ui、Input 接口；不得重新定义阶段 11/12 的 `ActorCatalog` 数据模型。
- `IPevtActorCatalogService` 只返回阶段 11/12 的中立人物资料投影、视觉引用和 anchor；不向 Core/组合处理器暴露 EvPerson、PxlCharacter、MImage 或 Unity 类型。
- 定义事件会话快照、临时占用登记与持久变更边界。
- 提供完整内存替身和调用记录器，作为后续组合处理器测试基座。
- 不在接口中暴露 Unity 或游戏内部对象。
- 验证：接口程序集引用检查、内存人物解析、临时资源逆序恢复和持久状态不回滚测试。

### 阶段 28：对话与选择 P0 处理器（850–1000 行）

- 实现 @say、@narrate、@board、talker、dialogue、skip 与 choose/choice 系列组合；@say 必须先把 `aic:noel` 等 ID 解析为人物 profile。
- 严格按同步中间层规范的原子顺序运行。
- 选择序号从 1 开始，高级选择返回稳定 key，失败时始终 Reset。
- 接入真实游戏适配器的最窄对话/选择入口。
- 验证：内存服务顺序快照、未知人物 PEVTR4401、临时 talker 覆盖恢复和可人工运行的最小 Galgame 对话事件。

### 阶段 29：PolarisRes 原版 PXLS 借用桥与人物视觉解析（900–1000 行）

- 在 PolarisRes 增加只读 `game-pxls` 提供者，以逻辑 Bundle 路径调用原版 MTI/MTRX/PxlsLoader 链并投影为统一只读 handle。
- 禁止绝对路径和任意 Bundle 类型；借用句柄不拥有原版 PxlCharacter/MImage，释放只撤销 PolarisEvent 自己的引用。
- 实现 `polaris-res` 延迟字段访问器适配：PXLS 接受 PxlsCharacterHandle，UI portrait 接受 MImage，并转换为中立 visual lease。
- 对 Ready/Faulted、事件取消、重复借用、原版 loader 已存在和插件卸载建立 PevtResourceWait 与清理规则。
- 验证：真实 `EvImg/__ev_n`、`PxlNoel/noel`、一个 MapChars 样本，缺失资源 PEVTR4403，且白名单扫描证明未调用 EvReader/EV.readOneLine。

### 阶段 30：角色立绘与通用图层 P0（900–1000 行）

- 实现 actor、image、cg、silhouette 全部 P0 组合处理器。
- 先从人物目录验证 actor、appearance、anchor、资源、动作和图层 ID，再产生副作用。
- 移动、渐变等返回 PevtWait；立即调用同样走统一例程。
- 接入游戏适配器：PXLS portrait 解析登记 pose/frame，UI MImage 复用 PUI Image 的 UV/缩放装配规则；登记事件拥有的图层和动作。
- 原版 `LegacyPerson` 只用于选择内置视觉源，不把 TALKER/PIC 文本交回原版解释器。
- 验证：`aic:noel` 与自定义 `.pactor` 各跑一套 enter/appearance/move/exit，资源失败零副作用，事件结束图层清理。

### 阶段 31：画面、镜头与后处理 P0（800–950 行）

- 实现 screen_fade、flash、camera、effect 和 spotlight 组合。
- 事件开始保存镜头/遮罩状态；reset 和结束清理恢复快照。
- 对 frames、opacity、zoom、easing 等参数执行域验证。
- 接入游戏侧最窄适配器。
- 验证：取消中途渐变、镜头恢复、同资源占用冲突和失败诊断。

### 阶段 32：音频、UI 与输入 P0（900–1000 行）

- 实现 sound、voice、ambience、music、UI、title、tutorial 和 input 系列。
- 资源预载与等待使用 ResourceWait；BGM/环境声生命周期按会话登记。
- UI/输入临时修改结束时恢复，持久游戏设置不在此层改变。
- 完成 P0 API 真实适配器注册。
- 验证：P0 全表 Descriptor 均有处理器，结束/异常/取消后无 UI、输入或音频泄漏。

### 阶段 33：公开 PolarisEvent 宿主与插件循环（800–950 行）

- 在现有 `PolarisEventComponent` 和唯一 `PolarisCore` 宿主契约上实现 Start、Change、Stop 和 PevtEventInstance 引用，不新增第二个组件循环或 `BepInPlugin`，也不伪装成原版 GameEvent。
- Plugin 初始化时先登记内置人物，再扫描人物与事件注册器；Unity 更新点按固定顺序推进调度器。
- Change 先取消并完成旧根事件清理，再启动新事件。
- 提供状态、诊断和所有权树只读查询。
- 验证：启动 P0 示例、内置/自定义人物目录可查、替换、主动停止、插件卸载和无事件空转。

### 阶段 34：async @/_ 与 handler 基础（900–1000 行）

- 把 async @ 与 async _ 调用启动为事件拥有的独立子例程。
- handler 只保存调度器 ID 与预期返回类型，不复制任务状态。
- 实现 status、单句柄 await 和 kill；异常完成时不赋值。
- 同步与异步调用必须使用完全相同的 CommandRoutine/块执行定义。
- 验证：并行推进、完成赋值、kill、异常句柄和丢弃句柄警告。

### 阶段 35：await all/any 与取消规则（850–1000 行）

- 实现 all/any 参数完整性、返回变量全写或全省略和索引从 1 开始。
- all 返回正常结束数量；any 返回首个正常完成序号，全部异常返回 0。
- any 选出胜者后 kill 未完成项；同帧按稳定调度 ID 决胜。
- 实现未观察异步异常 PEVTR5005 和事件结束自动 kill。
- 验证：成功/异常/取消混合矩阵、同帧顺序和返回值未初始化规则。

### 阶段 36：callevt 与事件所有权（850–1000 行）

- 同步 callevt 在执行时查 /event 并压入子事件帧；调用者等待结束。
- enable async 目标作为子事件启动并返回 handler。
- 缺失、冲突、非异步目标和启动失败映射 PEVTR4301–4304。
- 限制调用深度；父结束时清理全部异步子事件。
- 验证：跨模拟程序集、递归、运行时晚注册、缺失 ID 静态通过而运行失败。

### 阶段 37：exec 动态片段（800–950 行）

- 运行时用 Core 解析绑定字符串片段，禁止 id、enable、块定义、end、标签和 goto。
- 片段可读写允许的外层变量，但新增变量只存在临时环境。
- 继承宿主能力，不允许动态扩大 cs/async 权限。
- 实现独立深度和步数限制，内部静态诊断包装为 PEVTR1201。
- 验证：环境读写、临时变量消失、嵌套深度、动态 raw 能力和禁止语句。

### 阶段 38：$raw cmd 独占桥（750–900 行）

- 只在 Polaris/Event/Pevt/RawCmd 下实现原版 EV 文本会话适配器。
- 同一时间只允许一个 raw cmd 会话，串行等待原版前台状态机完成。
- 支持取消、错误翻译、会话清理和事件所有权；不写永久 .cmd 缓存。
- 不允许普通 @ 或 PEVT 事件进入 EV.readOneLine。
- TALKER_REPLACE 及其 `mb/x/a` 等临时别名只存在于本 raw 会话；结束和取消时恢复原版人物资料，绝不写入 PevtActorRegistry。
- 验证：并发排队、解析失败 PEVTR4101、取消与后续会话可继续。

### 阶段 39：$raw cs 分析、包装与缓存（900–1000 行）

- 实现受信任 C# 执行器接口和 Roslyn 语句包装，显式注入普通值副本。
- 工具侧与运行时共用返回类型、所有路径 return 和参数名校验。
- 编译缓存键包含代码、参数名/类型、引用集合与语言版本。
- 只接受 int、float、bool、char、string 或 void；null 结果在边界拒绝。
- 验证：PEVT8007–8015、五种返回类型、无 return、异常和缓存命中。

### 阶段 40：$raw cs 运行隔离与 exec 支持（700–900 行）

- 运行时在 enable cs 条件下执行缓存委托，输入和输出都进行值复制。
- exec 中出现 raw cs 时使用同一执行器动态编译，不扩大宿主引用集合。
- 捕获异常为 PEVTR4102；return 只退出当前 C# 块。
- 加入源码映射，使 C# 编译/运行诊断指向对应 raw 块。
- 验证：局部修改不回写、动态编译、能力拒绝、并发缓存和卸载释放。

### 阶段 41：地图、天气与场景实体 P1（900–1000 行）

- 增加 World 与 Entity 真实适配器并实现 API 表全部组合。
- 地图切换登记 AbortTransition 清理，成功结束后撤销。
- 可预期的对象不存在按 API 返回 bool；底层失效才产生 PEVTR4001。
- 实体移动和动作均使用受管 Wait，不接受 MoveScript 字符串。
- 验证：地图切换取消、实体消失、动作超时和所有 P1 Descriptor 覆盖。

### 阶段 42：状态、物品与进度 P1（900–1000 行）

- 实现 State、Inventory、Quest 与 autosave 真实适配器和全部组合。
- 所有 ID、范围与资源校验必须发生在首个持久写操作前。
- 持久写入不随事件失败回滚；临时通知 UI 仍属于会话清理。
- counter_add 使用 checked 并返回新值。
- 验证：状态快照、失败零部分写入、持久值保留和存档等待。

### 阶段 43：Alice In Cradle 领域 P2（700–900 行）

- 实现 Player 与 Battle 适配器及 P2 全部 API。
- 领域对象查找失败按签名返回 bool，未预期游戏异常进入 PEVTR4001。
- 战斗动作与召唤状态使用可取消 Wait，事件结束不残留动作所有权。
- 通用 PEVT Core 不引用这些领域类型。
- 验证：P2 描述器全覆盖、取消、对象销毁和普通项目不注册 P2 时的明确诊断。

### 阶段 44：PolarisTools `.pevt` 内容类型、高亮与实时诊断（900–1000 行）

- 新建 .pevt ContentType、分类、格式、tagger 和错误列表数据源。
- 高亮可使用轻量 token 快照，但实时诊断必须调用 Core，不复制语法规则。
- 实现文档版本取消、防抖和过期结果丢弃。
- 显示准确 PEVT 编号、级别、文件、行列和消息。
- 验证：VS MEF 组合测试、快速连续编辑、raw 块与多行字符串着色。

### 阶段 45：PolarisTools `.pactor` 参数编辑器与资源选择器（900–1000 行）

- 新增 `.pactor` XML 自定义编辑器，字段只覆盖人物资料、WorldSprite、Portrait、UiPortrait、Appearance 与 Anchor，不提供脚本或任意 C# 输入框。
- 复用阶段 18 抽取的 PolarisRes 目录与 PUI Image 缩略图逻辑，分别筛选 PxlsCharacterHandle/MImage；失效引用可见、可修复但不被静默清空。
- 支持增加/删除人物与 visual、默认 portrait 选择、pose/frame 文本、颜色、voice、anchor 数值和实时 PEVT91xx 错误列表。
- 写回稳定、可读的 UTF-8 XML，保留未知非执行性扩展节点以便版本升级；BuiltIn 目录在编辑器中只读。
- 验证：编辑保存重开等价、资源增删 watcher 刷新、错误引用显示、XML 转义、撤销/重做和固定目录不可改。

### 阶段 46：补全、快速信息与导航（900–1000 行）

- 基于绑定上下文补全关键字、变量、块、@ 重载、参数名和参数域；人物参数合并内置目录与当前项目 `.pactor`。
- 快速信息展示完整 @ 签名、返回值、等待方式和能力要求。
- 实现变量/块定义跳转与引用查找；actor/appearance 可跳到 `.pactor`，callevt 和跨模组 actor 不假设目标必在当前项目。
- 所有结果绑定到文档版本并支持取消。
- 验证：作用域先后顺序、重载提示、`aic:noel`/自定义人物与 appearance 补全、中文源码位置和跨文档失效。

### 阶段 47：格式化、模板与调试查看（800–950 行）

- 实现非强制格式化：每嵌套一层四空格，保留 raw 内容和多行字符串列对齐。
- 更新 .pevt 模板为合法最小事件和使用 `aic:noel` 的 P0 演出示例；增加自定义人物 `.pactor` + PolarisRes 字段示例。
- 提供只读运行事件、调用栈、handler、Wait、预算和所有权树查看。
- 调试查看不得改变正常 Tick 顺序。
- 调试视图同时显示解析后的人物 ID、portrait/appearance、视觉来源和资源等待，但不暴露可变游戏对象。
- 验证：格式化幂等、语义前后一致、raw 字节不变、模板可编译和关闭调试零差异。

### 阶段 48：端到端硬化与发布门（900–1000 行）

- 建立从 .pevt 生成 C#、编译模拟模组、扫描注册、解释运行到清理的端到端测试。
- 覆盖 P0 完整演出、P1 地图剧情、P2 领域回调、async、callevt、exec 和两个 raw 桥。
- 覆盖 18 个原版稳定说话人短键固定映射（含无立绘的 `_` 叙述者）、TALKER_REPLACE 隔离、一个自定义 `.pactor`、原版 game-pxls 与自定义 PolarisRes PXLS/MImage。
- 加入 lexer/parser 模糊测试、深度/大小/解压上限、长事件分帧预算和调度确定性回放。
- 对所有静态/运行诊断做编号覆盖检查，对全部 API Descriptor、人物 provider 和生成注册器做覆盖检查。
- 最终旧链路扫描除 RawCmd 白名单外必须为零；普通演出测试不得加载原版 EV。
- 验证：Core、Polaris、PolarisTools、集成测试全绿，并输出人物目录冲突、性能与资源清理报告。

## 9. 各阶段交付模板

Claude 每次完成阶段后按以下格式汇报：

1. 阶段编号与目标。
2. 有效变更行数；说明是否位于 600–1000。
3. 新增、修改、删除文件列表。
4. 对应规范条目和诊断编号。
5. 执行的构建与测试命令及结果。
6. 附上 2.1 的完整证据矩阵，所有要求逐行给出生产代码与测试定位。
7. 附上诊断覆盖清单：目录存在、实际发射、正向测试、反向测试、负责阶段。
8. 附上 2.2 的反向审计结果及发现的近似替代风险。
9. 该阶段退出条件逐项结论；存在任意 `FAIL` 时结论只能是“阶段未完成”。
10. 仍留给下一阶段的内容；每项必须原本就在后续阶段范围内，不能把当前阶段遗漏事后顺延。

阶段验证失败时只修复当前阶段。不得通过跳过测试、删除诊断、降低断言或恢复旧 .cmd 执行链来获得绿色结果。

## 10. 最终验收

- 普通 Galgame 对话、选择、立绘、图层、画面、音频和 UI 完全由可读 @ 指令执行。
- 原版人物使用 `aic:noel` 等固定可读 ID；模组人物只需 `.pactor` 与 PolarisRes 资源字段即可参与同一套对话和演出。
- 除 `$raw cmd` 会话兼容外，普通代码和公开参数中不出现 `n/a/so` 等人物短键或 TALKER_REPLACE 临时键。
- PEVT 角色演出、地图实体、状态进度和 C# 回调均经过受控服务，不暴露任意游戏 API。
- 工具侧和游戏侧对同一源码得到一致 AST、绑定结果和 PEVTxxxx。
- 同步、异步、事件调用、替换、异常和卸载都能证明所有权与清理结果。
- 运行错误能回溯到事件 ID、模组、相对路径、源码行列和事件/块/协程调用栈。
- 除显式 $raw cmd 外，代码和测试中不存在 .cmd 生成、磁盘 events 缓存、EvReader 或 EV 默认分派依赖。
- 新系统保持解释型：嵌入的是压缩原始文本，不是 PEVT 字节码或原版命令文本。
